# Streamlit 웹 UI 실행 가이드

## 문제: 브라우저가 연결되지 않는 경우

### 해결 방법 1: 올바른 실행 순서

1. **터미널/PowerShell을 엽니다**
2. **프로젝트 폴더로 이동합니다**
   ```
   cd "C:\Users\User\Desktop\연세 3-2\인사이더스\MVP\아이데이션\말로 쓰는 일기\리뷰 데이터 분석"
   ```

3. **Streamlit 실행:**
   ```
   streamlit run app.py
   ```

4. **터미널에서 "You can now view your Streamlit app in your browser"라는 메시지가 나올 때까지 기다립니다**
   - 일반적으로 다음 주소가 표시됩니다:
     ```
     Local URL: http://localhost:8501
     Network URL: http://192.168.x.x:8501
     ```

5. **브라우저를 열고 표시된 주소로 접속합니다**
   - `http://localhost:8501` 입력
   - 또는 터미널에 표시된 URL을 복사해서 붙여넣기

### 해결 방법 2: 배치 파일 사용

1. 파일 탐색기에서 `run_ui.bat` 파일을 찾습니다
2. **더블클릭**하여 실행
3. **명령 프롬프트 창이 열리고 Streamlit이 시작됩니다**
4. "You can now view your Streamlit app" 메시지가 나타날 때까지 **기다립니다**
5. 브라우저가 자동으로 열리지 않으면, 수동으로 `http://localhost:8501`로 접속

### 해결 방법 3: 다른 포트 사용

포트 8501이 이미 사용 중인 경우:

```
streamlit run app.py --server.port 8502
```

그리고 브라우저에서 `http://localhost:8502`로 접속

## 중요 사항

⚠️ **Streamlit 서버가 완전히 시작될 때까지 기다려야 합니다!**
- 서버 시작에는 몇 초가 걸릴 수 있습니다
- 터미널에 "You can now view..." 메시지가 나타나기 전에는 브라우저가 연결되지 않습니다

## 체크리스트

- [ ] Streamlit이 설치되어 있는가? (`pip install streamlit`)
- [ ] 터미널에서 에러 메시지가 없는가?
- [ ] 서버가 완전히 시작되었는가? (터미널 메시지 확인)
- [ ] 올바른 주소로 접속하고 있는가? (`http://localhost:8501`)

## 문제가 계속되면

1. **터미널을 완전히 닫고 다시 시작**
2. **방화벽 확인**: Windows 방화벽이 Python을 차단하지 않는지 확인
3. **포트 확인**: 다른 프로그램이 8501 포트를 사용하고 있는지 확인
4. **다른 브라우저 시도**: Chrome, Edge, Firefox 등







