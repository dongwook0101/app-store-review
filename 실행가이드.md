# 실행 가이드

## 문제 상황
PowerShell에서 한글 경로로 인한 인코딩 문제가 발생할 수 있습니다.

## 해결 방법

### 방법 1: 파일 탐색기에서 직접 실행 (가장 간단) ⭐

1. 파일 탐색기(Windows 탐색기)를 엽니다
2. 다음 경로로 이동:
   `C:\Users\User\Desktop\연세 3-2\인사이더스\MVP\아이데이션\말로 쓰는 일기\리뷰 데이터 분석`
3. **`run.bat`** 파일을 더블클릭하여 실행

### 방법 2: 파일 탐색기에서 PowerShell 열기

1. 파일 탐색기에서 위 폴더를 엽니다
2. 주소창에 `powershell` 또는 `cmd` 입력 후 **Enter** 키 누르기
3. 열린 터미널에서 다음 명령 실행:
   ```bash
   python analyze_reviews.py
   ```

### 방법 3: 절대 경로로 실행

현재 위치(`C:\Users\User>`)에서 다음 명령을 실행하세요:

```powershell
python "C:\Users\User\Desktop\연세 3-2\인사이더스\MVP\아이데이션\말로 쓰는 일기\리뷰 데이터 분석\analyze_reviews.py"
```

### 방법 4: Python IDLE 또는 VS Code 사용

1. 파일 탐색기에서 `analyze_reviews.py` 파일을 찾습니다
2. 파일을 마우스 오른쪽 버튼으로 클릭
3. "연결 프로그램" > "Python" 선택 (또는 VS Code로 열기)
4. VS Code나 Python IDLE에서 F5 키를 눌러 실행

---

## 추천 방법
**방법 1 (run.bat 더블클릭)**이 가장 간단하고 확실합니다!







