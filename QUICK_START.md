# 🚀 빠른 시작 가이드

## ✅ 완료된 작업
1. ✅ Google OAuth 클라이언트 정보 설정 완료
2. ✅ 필요한 패키지 설치 완료

## ⚠️ 필수 확인 사항: Google Cloud Console 설정

앱을 실행하기 전에 **반드시** 다음을 확인하세요:

### 1. 리디렉션 URI 설정 (필수!)
1. [Google Cloud Console](https://console.cloud.google.com/) 접속
2. 프로젝트 선택
3. **API 및 서비스** > **사용자 인증 정보** 클릭
4. 클라이언트 ID (`403626382842-512spe9nk3dvj2omjj2iqe1880k39btu.apps.googleusercontent.com`) 클릭
5. **승인된 리디렉션 URI** 섹션 확인
6. 다음 URI가 **반드시** 추가되어 있어야 합니다:
   ```
   http://localhost:8501
   ```
   - 없으면 **+ URI 추가** 버튼 클릭하여 추가
   - 저장 클릭

### 2. OAuth 동의 화면 확인
1. **API 및 서비스** > **OAuth 동의 화면** 이동
2. 사용자 유형이 설정되어 있는지 확인
3. 테스트 중인 경우, **테스트 사용자**에 본인 이메일 추가

## 🎯 앱 실행

### 방법 1: 배치 파일 사용 (가장 간단)
1. `run_ui.bat` 파일 더블클릭
2. 브라우저에서 `http://localhost:8501` 접속

### 방법 2: 명령어로 실행
```bash
streamlit run app.py
```

## 🔐 로그인 테스트

1. 앱 실행 후 로그인 페이지가 표시됩니다
2. **"🔵 Google로 로그인"** 버튼 클릭
3. Google 계정 선택
4. 권한 승인
5. 로그인 완료 후 앱스토어 리뷰 분석 도구 사용 가능!

## ❌ 문제 발생 시

### "리디렉션 URI 불일치" 오류
→ Google Cloud Console에서 리디렉션 URI가 정확히 `http://localhost:8501`로 설정되어 있는지 확인

### "접근 거부됨" 오류  
→ OAuth 동의 화면에서 테스트 사용자로 본인 이메일 추가

### 로그인 버튼이 보이지 않음
→ 클라이언트 ID와 시크릿이 올바르게 설정되었는지 확인 (이미 설정됨)

## 📝 다음 단계

로그인이 성공하면:
- 사이드바에 로그인한 이메일이 표시됩니다
- 앱스토어 리뷰 분석 기능을 사용할 수 있습니다
- 로그아웃 버튼으로 언제든지 로그아웃 가능합니다


